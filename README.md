```zsh
# activate venv
$ . venv/bin/activate

# deactivate venv
deactivate
```

```zsh
# install packages
pip install -r requirements.txt

# update package list
pip install <package-name>
pip freeze > requirements.txt
```

```zsh
jupyter lab
```
